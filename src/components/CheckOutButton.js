import React from 'react';
import './CheckOutButton.css'; // Import the CSS file for the button

const CheckoutButton = () => {
  return <button className="checkout-button">Check Out</button>;
};

export default CheckoutButton;