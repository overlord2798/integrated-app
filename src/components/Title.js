import React from 'react';
import './Title.css'; // Import the CSS file for the title

const Title = () => {
  return (
    <h2 className="title">Hardware Rescource Management</h2>
  );
};

export default Title;