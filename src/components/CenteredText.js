import React from 'react';
import './CenteredText.css'; // Import the CSS file for the component

const CenteredText = ({ text }) => {
  return <div className="centered-text">{text}</div>;
};

export default CenteredText;